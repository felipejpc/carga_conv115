<%= form_for(@mestre_documento_fiscal) do |f| %>
  <% if @mestre_documento_fiscal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mestre_documento_fiscal.errors.count, "error") %> prohibited this mestre_documento_fiscal from being saved:</h2>

      <ul>
      <% @mestre_documento_fiscal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
